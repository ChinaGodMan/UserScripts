name: "reminder"

on:
  issue_comment:
    types: [created, edited]

permissions:
  contents: read

jobs:
  reminder:
    permissions:
      issues: write # for agrc/create-reminder-action to set reminders on issues
      pull-requests: write # for agrc/create-reminder-action to set reminders on PRs
    runs-on: ubuntu-latest

    steps:
      - name: check for reminder
        uses: agrc/create-reminder-action@64196866d8d89ce172328d200d15afb8533b7992 # v1.1.24
